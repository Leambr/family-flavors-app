export interface RecipeCardProps {
    recipeName: string;
    description: string;
    dietType: string;
    isHeaderCarousel?: boolean;
    imageUrl?: string;
    isActive?: boolean;
    isDescription?: boolean;
}
